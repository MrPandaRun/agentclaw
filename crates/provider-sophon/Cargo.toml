[package]
name = "provider-sophon"
version = "0.1.0"
edition = "2021"
description = "Sophon adapter for AgentDock"

[dependencies]
provider-contract = { path = "../provider-contract" }
serde = { workspace = true }
serde_json = { workspace = true }
